package com.hospital.booking.constants;

public class AppointmentConstants {
    // Prevent cancel order update appointment before start time
    public static final int BLOCK_APPOINTMENT_BEFORE = 60; //minutes
}
